%--- Part 1: Motivation (4 min) -------------------------------------------------

\section{Motivation}

%--- Slide: Trajectory Prediction for Autonomous Driving -----------------------
\begin{frame}{Trajectory Prediction for Autonomous Driving}
  \begin{columns}
    \begin{column}{0.55\textwidth}
      \begin{itemize}[<+->]
        \item \textbf{Task:} forecast future $(x, y)$ positions of agents
              (pedestrians, cyclists, vehicles) from observed past movements
        \item Challenging due to \textbf{complex agent interactions} and influence of \textbf{static context}
        \item Essential for \textcolor{lmugreen}{\textbf{safe path planning}} and \textcolor{lmugreen}{\textbf{collision avoidance}}
              in autonomous vehicles
      \end{itemize}
    \end{column}
    \begin{column}{0.42\textwidth}
      \begin{center}
        % TODO: nuScenes scene figure with trajectory predictions overlaid
        \textit{[figure placeholder]}
      \end{center}
    \end{column}
  \end{columns}
\end{frame}

%--- Slide: Trajectron++ -------------------------------------------------------
\begin{frame}{Trajectron++}
  \begin{columns}
    \begin{column}{0.52\textwidth}
      \begin{itemize}[<+->]
        \item State-of-the-art probabilistic trajectory prediction model
              \textnormal{\small[Salzmann et al., 2020]}
        \item \emph{Spatio-temporal graph encoder}
        \begin{itemize}
          \item LSTM per agent 
          \item edges encode agent--agent interactions
        \end{itemize}
        \item \emph{CVAE decoder}
        \begin{itemize}
          \item Outputs a distribution over future
              trajectories
        \end{itemize}
        \item Handles \emph{heterogeneous} agent types jointly
              (pedestrian, bicycle, car) and integrates HD map context
      \end{itemize}
    \end{column}
    \begin{column}{0.45\textwidth}
      \onslide<2->{
        \textbf{trajdata} \textnormal{\small[NVLabs]}\\[0.4em]
        \begin{itemize}
          \item Unified interface to many trajectory datasets
                (nuScenes, ETH/UCY, \ldots)
          \item[$\Rightarrow$] Our analysis pipeline applies to
                \emph{any} trajdata-supported dataset
        \end{itemize}
      }
    \end{column}
  \end{columns}
\end{frame}

%--- Slide: nuScenes -----------------------------------------------------------
\begin{frame}{nuScenes Dataset}
  \begin{columns}
    \begin{column}{0.50\textwidth}
      \begin{itemize}[<+->]
        \item 1\,000 urban driving scenes, Boston \& Singapore
        \item Agent types: pedestrians, cyclists, vehicles
        \item Rich sensor suite: camera, LiDAR, radar, GPS, HD map
        \item Standard split: 700 / 150 / 150 scenes
              (train / val / test)
        \item \textbf{Scope:} we predict \emph{pedestrian} trajectories
              only, while all agent types serve as scene context
      \end{itemize}
      \vspace{0.5em}
      \onslide<6->{
        \textbf{Prediction metrics:}\\[0.3em]
        \begin{tabular}{ll}
          \texttt{ml\_ade}   & most-likely ADE \textcolor{lmugreen}{\textbf{$\leftarrow$ used}}\\
          \texttt{ml\_fde}   & most-likely FDE \\
          \texttt{min\_ade\_5} & best-of-5 ADE \\
          \texttt{nll\_mean} & negative log-likelihood \\
        \end{tabular}
      }
    \end{column}
    \begin{column}{0.47\textwidth}
      \begin{center}
        % TODO: nuScenes scene image or trajectory plot
        \textit{[figure placeholder]}
      \end{center}
    \end{column}
  \end{columns}
\end{frame}

%--- Slide: Research Questions -------------------------------------------------
\begin{frame}{Challenges and Research Questions}
  \begin{columns}
    \begin{column}{0.52\textwidth}
      \begin{itemize}[<+->]
        \item Some trajectories are inherently hard, others trivially
              easy---but \emph{why?}
        \item A single average ADE/FDE gives no insight into reasons
              for failure and success
        \item No understanding of which trajectory or scene characteristics
              drive per-trajectory prediction error
      \end{itemize}
      \vspace{0.4em}
      \onslide<4->{$\Rightarrow$ \textbf{\textcolor{lmugreen}{Three research questions:}}}
    \end{column}
    \begin{column}{0.45\textwidth}
      \begin{enumerate}
        \item<5-> Which trajectory and scene characteristics affect model
              performance?
        \item<6-> What are the \emph{success and failure modes} that explain
              model performance?
        \item<7-> How do model settings (history window, prediction horizon,
              attention radius) affect performance?
      \end{enumerate}
    \end{column}
  \end{columns}
\end{frame}
