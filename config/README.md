# Configuration Directory

Maintain experiment and environment configuration files supporting the centralized Trajectron++ workflow.

## Suggested Contents
- YAML/JSON templates describing dataset locations, clustering parameters, and model settings.
- Federated-learning-oriented notes for future adaptations of Trajectron++.
- Logging and reproducibility configuration (random seeds, output directories, etc.).

## Next Steps
- Create a base configuration template capturing defaults for data preprocessing and training.
- Document how to override configurations per cluster subset or experiment round.
