# Shared attention radii (in meters) for agent interactions.
# Used by training, joined-metrics processing, and notebooks.

attention_radius:
  default: 20.0
  pairs:
    PEDESTRIAN:
      PEDESTRIAN: 10.0
      VEHICLE: 20.0
    VEHICLE:
      PEDESTRIAN: 20.0
      VEHICLE: 30.0
